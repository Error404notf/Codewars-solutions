function disemvowel(str) {
  // Parameters str
  //Return new word with no vowels
  //Examples
  //Pseudocode
  
  //retrieve a string
  
  // loop through the string
  // remove vowels
  // display the new word
  return str.replace(/[aeiouAEIOU]/g, '')
}