function rowSumOddNumbers(n) {
  // TODO
//  const tsplit = n.toString().split('')
//  const total = tsplit.reduce((a,b)=> Number(a) + Number(b))
//  return total
  //Parameters = n
  //Return = sum based off the row (N)
  //Examples = rowSumOddNumbers(42), 74088
  //Pseudocode
  // sumation of numbers in a specific row
  return n*n*n;
}