function solution(str, ending){
  // Parameters = str, ending
  // Return = true = str ends with ending
  //Examples = 'abcde', 'cde', true 
  //Pseudocode
  // if str.endsWith(ending) ? true : false
  return str.endsWith(ending) ? true : false
}