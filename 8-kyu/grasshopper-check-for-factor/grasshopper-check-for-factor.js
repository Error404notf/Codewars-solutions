function checkForFactor (base, factor) {
  // Parameter = base and factor
  //Return = if base and factor is % true or false
  //Example =  (6,3) true (4,5) false
  //Pseudocode 
     // extract base and factor
     // if base % factor == 0 ? true : false
  
   return base % factor == 0 ? true : false
}