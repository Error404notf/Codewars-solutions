function sumStr(a,b) {
   //Parameters = a,b
  //Return = return the sum of the parameters as strings
  //Examples  = (sumStr("4","5"), "9")
  //Pseudocode
  // convert the two parameters into numbers
  // add them  up then convert to string
//   let convertA = Number(a)
//   let convertB = Number(b)
//   return String(convertA + convertB)
  return (+(a) + +(b)).toString()
}