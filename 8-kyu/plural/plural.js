function plural(n) {
  //Parameter = n
  //Return = false for 1 and the rest is true
  //Examples = (0), true, "Plural for 0"
  //Pseudocode
  // if n == 1 it will be false for other numbers it will be true
  return n === 1 ? false : true
}